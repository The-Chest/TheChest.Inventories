name: Publish library  

on:
  push:
     tags:        
      - "**"

permissions:
  contents: write

env:
  PROJECT_NAME: "TheChest.Inventories"
  SOURCE_DIR: "src"
  RELEASE_OUTPUT_DIR: "src/bin/Release/netstandard2.1"


jobs:
  build:
    name: "Build Project"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
  
      - name: Get Current Version
        id: get-version
        uses: The-Chest/Actions/get-version@v1

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '6.0.x'
    
      - name: Dotnet Build
        id: dotnet-build
        uses: The-Chest/Actions/dotnet-build@v1
        with:
          project-path: ${{ env.SOURCE_DIR }}
          create-build-artifact: true
          artifact-name: "${{ env.PROJECT_NAME }}.${{ steps.get-version.outputs.version }}"
          artifact-folder: "${{ env.RELEASE_OUTPUT_DIR }}"
  
  publish-nuget:
    name: "Publish on NuGet"
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
  
      - name: Get Version
        id: get-version
        uses: The-Chest/Actions/get-version@v1

      - name: Download Build Artifact
        uses: actions/download-artifact@v6
        with:
          name: "${{ env.PROJECT_NAME }}.${{ steps.get-version.outputs.version }}"
          path: "${{ env.RELEASE_OUTPUT_DIR }}"

      - name: Restore dependencies
        run: dotnet restore ${{ env.SOURCE_DIR }}

      - name: Publish NuGet package
        uses: The-Chest/Actions/publish-nuget@v1
        with:
          package-path: ${{ env.SOURCE_DIR }}
          package-name: ${{ env.PROJECT_NAME }}
          package-version: ${{ steps.get-version.outputs.version }}
          nuget-api-key: ${{ secrets.NUGET_API_KEY }}
                
      - name: Upload Nuget to release
        uses: The-Chest/Actions/upload-release-artifacts@v1
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"
          path: "./"
          name: "${{ env.PROJECT_NAME }}.${{ steps.get-version.outputs.version }}.nupkg"
          version: "${{ steps.get-version.outputs.version }}"

  publish-gh-pack:
    name: "Publish on Github Packages"
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
  
      - name: Get Version
        id: get-version
        uses: The-Chest/Actions/get-version@v1

      - name: Download Build Artifact
        uses: actions/download-artifact@v6
        with:
          name: "${{ env.PROJECT_NAME }}.${{ steps.get-version.outputs.version }}"
          path: "${{ env.RELEASE_OUTPUT_DIR }}"

      - name: Restore dependencies
        run: dotnet restore src
      
      - name: Push library to Github Packages
        run: dotnet nuget push ${{ env.PROJECT_NAME }}.${{ steps.get-version.outputs.version }}.nupkg --source https://nuget.pkg.github.com/The-Chest/index.json --api-key "${{secrets.GH_NUGET_APIKEY}}"
  
  publish-dll-file:
    name: "Publish DLL files"
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
  
      - name: Get Version
        id: get-version
        uses: The-Chest/Actions/get-version@v1

      - name: Download Build Artifact
        uses: actions/download-artifact@v6
        with:
          name: "${{ env.PROJECT_NAME }}.${{ steps.get-version.outputs.version }}"
          path: "${{ env.RELEASE_OUTPUT_DIR }}"
      
      - name: Upload DLL to release
        uses: The-Chest/Actions/upload-release-artifacts@v1
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"
          path: "${{ env.RELEASE_OUTPUT_DIR }}"
          name: "${{ env.PROJECT_NAME }}.${{ steps.get-version.outputs.version }}.dll"
          version: "${{ steps.get-version.outputs.version }}"
  
  publish-src-files:
    name: "Publish Source Files"
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
  
      - name: Get Version
        id: get-version
        uses: The-Chest/Actions/get-version@v1

      - name: Zip Folder
        run: zip -r "${{ env.PROJECT_NAME }}.${{ steps.get-version.outputs.version }}.zip" src/ -i "*.cs"
        
      - name: Upload Source Files to release
        uses: The-Chest/Actions/upload-release-artifacts@v1
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"
          path: "./"
          name: "${{ env.PROJECT_NAME }}.${{ steps.get-version.outputs.version }}.zip"
          version: "${{ steps.get-version.outputs.version }}"
